{
  "name": "pdf-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm --prefix server start\" \"npm --prefix client dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "install:all": "npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
